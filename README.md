# XMedia Converter

Local file converter (video/audio/image) built with Next.js.
Audio/video conversions require **FFmpeg**. Images work without FFmpeg.

## Requirements
- Node.js 18+ (recommended 20)
- pnpm
- FFmpeg (for audio/video)

## Install
```bash
pnpm install
pnpm dev
